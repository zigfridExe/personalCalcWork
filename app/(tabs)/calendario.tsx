// Tab Calendário: renderiza a tela principal do calendário já alinhada ao novo fluxo
import CalendarioScreen from '../calendario/index';

export default function CalendarioTab() {
  // Nenhuma lógica antiga, apenas renderização direta
  return <CalendarioScreen />;
}
